import LeadForm from "@/components/LeadForm";

export default function ContactPage() {
  return (
    <main style={{ paddingTop: "100px", minHeight: "80vh", background: "#020617" }}>
      <div style={{ padding: "60px 0" }}>
        <LeadForm />
      </div>
    </main>
  );
}
